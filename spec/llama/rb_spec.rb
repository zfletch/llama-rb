# frozen_string_literal: true

RSpec.describe Llama::Rb do
  it "has a version number" do
    expect(Llama::Rb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
